// global scope = defined outside of all code blocks
// local scope - defined inside of a code block

// in a scope you can access all variables defined in that scope and in a parent/ancestor scope.

//fantastic definition of scope in andrew meade's variable scope section

// variable shadowing- when a variable in a local scope overeshaadows the existence of a varialbe in the parent scope

// leaked global - when we don't explicity decalre a vairable, but when we still reassign to something, js will assing it to the global scope.